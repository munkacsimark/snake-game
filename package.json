{
  "name": "snakegame",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^14.5.0",
    "npm-watch": "^0.2.0",
    "tsify": "^3.0.4",
    "typescript": "^2.9.1"
  },
  "watch": {
    "build": {
      "patterns": [
        "src"
      ],
      "extensions": "ts"
    }
  },
  "scripts": {
    "build": "npm run html && npm run compile",
    "html": "cp src/index.html dist",
    "compile": "browserify src/main.ts -p tsify --noImplicitAny > dist/snake.js",
    "watch": "npm-watch"
  }
}
